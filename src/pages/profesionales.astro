---
import Layout from '../layouts/Layout.astro';
import PageHeader from '../components/ui/PageHeader.astro';
import TeamCard from '../components/TeamCard.astro';
import { teamData } from '../data/teamData';
---

<Layout 
  title="Equipo Profesional | Octubre Consultorio"
  description="Conocé a las profesionales de Octubre: especialidades, trayectoria y horarios de atención en Psicología, Psicopedagogía, Fonoaudiología y Terapia Ocupacional."
>

  <PageHeader 
    badge="Equipo Interdisciplinario"
    title="Profesionales comprometidos con tu bienestar"
    description="Un equipo en constante formación y articulación constante para ofrecerte una mirada integral, cercana y respetuosa de cada proceso."
  />

  <section class="py-12 sm:py-20">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
        {teamData.map((member) => (
          <TeamCard member={member} />
        ))}
      </div>
    </div>
  </section>

</Layout>